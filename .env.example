# No environment variables required for production.
# The app connects directly to the public Arkiv Braga RPC.
# All entity operations happen client-side via the browser wallet.

# Optional: override the default RPC endpoint
# NEXT_PUBLIC_ARKIV_RPC_URL=https://braga.rpc.arkiv.network
